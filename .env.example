# Defaults match the supplied Task 1 docker-compose.yml.
PGHOST=localhost
PGPORT=5432
PGDATABASE=olist_db
PGUSER=olist_user
PGPASSWORD=olist_pass

# Optional CSV override. The project checks data/raw and data automatically.
# OLIST_DATA_DIR=data/raw

